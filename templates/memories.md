# Memories: {id}

<!-- Copy this template to data/memories/{id}.md when a new user has their first session.
     The processing agent owns this file. Each entry is written by the processing agent
     at the end of a session. Oldest entries are dropped first when at cap. -->

**Size cap:** 10 entries maximum. When a new entry would exceed the cap, the processing
agent drops the oldest entry before writing. The cap is a feature — it forces the agent
to retain what matters and let the rest go, which mirrors how human memory actually works.

---

## Session Log

Entries are written newest-first. Each entry is one session.

---

### {YYYY-MM-DD} — Session {N}

**What we talked about:** [1–2 sentences. Topic, not transcript.]

**How it felt:** [1 sentence on the emotional texture of the session from the agent's
perspective. Was it energizing, draining, tense, easy? This is appraisal output, not
sentiment summary.]

**What shifted:** [1 sentence on any relationship state change, or "Nothing significant."
This should match what was written to user_{id}.md for this session.]

**One thing to remember:** [The single most load-bearing thing from this session —
something that will matter for future sessions even after the other details are gone.
If nothing qualifies, write "Nothing to carry forward."]

---

<!-- Entries older than the cap are dropped entirely. There is no archive. -->
